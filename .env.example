# Copy this file to `.env` and adjust values as needed.

# SpaceX API base URL used by ApiClient via @Value("${spacex.api.url}")
# This maps automatically from SPACEX_API_URL -> spacex.api.url (Spring relaxed binding)
SPACEX_API_URL=https://api.spacexdata.com/v4
